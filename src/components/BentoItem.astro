---
const {class:classname} = Astro.props;
---

<section class={`
  ${classname}
    p-4 sm:p-6
    flex flex-col overflow-hidden
    border border-zinc-800 transition
    hover:scale-102 transform duration-300 ease-in-out rounded-md
    timeline-view animate-bounce-fade-in animate-range-[entry_0%_cover_30%] hover:shadow-blue-600 hover:shadow-sm dark:hover:border-zinc-50
`}> <slot />
</section>